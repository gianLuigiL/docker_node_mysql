{
  "name": "node",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "docker:build": "docker-compose build",
    "docker:start": "docker-compose up",
    "dev": "cross-env ENV=dev PORT=5000 nodemon src/index.js",
    "prod": "cross-env ENV=prod node src//index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "nodemon": "^2.0.3",
    "reflect-metadata": "^0.1.13"
  },
  "devDependencies": {
    "cross-env": "^7.0.3"
  }
}
